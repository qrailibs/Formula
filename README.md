# Formula
Simple language for describing data formats, compilable to Regex

# Basic example
```formula
match+ 'A'
```
Compiled into regex:
```regex
A+
```

# Progress
- [X] `match` Statements
- [X] `group` Statements
- [X] `if` and `else` Statements
- [X] `define` Statements
